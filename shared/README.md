# Shared Resources

This directory is for assets reused by multiple projects.

Suggested categories:

- `templates/`
- `scripts/`
- `schemas/`
- `docs/`

Project-specific files stay under `projects/<slug>/`.
