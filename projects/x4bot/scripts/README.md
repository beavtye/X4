# Scripts

Script categories:

- `diagnostics/` — read-only investigation and reporting
- `migrations/` — explicit state/data migrations with backup and rollback
- `scheduled-tasks/` — scripts launched by Windows Task Scheduler
- `maintenance/` — controlled repository or runtime maintenance

Every script must document inputs, outputs, side effects, dry-run support, and rollback.
