# Configuration Templates

Only safe templates belong here.

Use names such as:

- `app.example.yaml`
- `agent-bridge.example.json`
- `mcp-server.example.json`

Templates may contain variable names, defaults, and setup instructions, but not live account identifiers, access keys, cookies, tokens, or machine-specific runtime state.
