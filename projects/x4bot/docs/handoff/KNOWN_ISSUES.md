# Known Issues

## WatchScan

Historical archives did not include bot outbound messages. Earlier no-reply results were therefore unreliable. A data-source fix and replay verification remain open.

## Watchdog

Automatic recovery is disabled until exact-process targeting and controlled tests are complete.

## Scheduled archive tasks

Two archive tasks exist. One runs normally and one has a path-related error. Any change requires user approval.

## Mimo host Git tools

The local Mimo host currently reports no Git or GitHub CLI installation.

## Repository visibility

The repository should be Private before implementation files or operational details are added.
