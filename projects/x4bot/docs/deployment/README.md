# Deployment Documentation

This folder documents installation, device migration, restoration, verification, and release packaging.

Implementation scripts live in `projects/x4bot/deploy/`. Documentation explains prerequisites, expected outputs, rollback, and validation.
