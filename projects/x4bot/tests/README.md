# Tests

Organize tests by component:

- `unit/`
- `integration/`
- `replay/`
- `fixtures/`

Fixtures must be synthetic or fully redacted. Runtime logs, chat archives, databases, and live credentials are never test fixtures.
