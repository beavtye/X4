# Projects

每个独立项目必须放在 `projects/<project-slug>/`。

## 已登记项目

- `x4bot/` — QQ Bot 运维与自动化
- `_template/` — 新项目模板

新增项目流程：

1. 复制 `_template/`
2. 建立 `PROJECT_MANIFEST.yaml`
3. 由 ChatGPT 审核目录和职责边界
4. 再由执行 Agent 填充代码与文档

禁止在仓库根目录直接堆放项目文件。
